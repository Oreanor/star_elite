/**
 * Единственный источник цвета интерфейса.
 *
 * Ретро, но БУДУЩЕЕ: пип-бой без зелёного фосфора. Зелёный — цвет уцелевшей
 * электроники прошлого; у нас корабельный дисплей, и он светит холодным голубым.
 *
 * Цвета жили копиями в пяти файлах, и «поправь худ» с «поправь меню» были двумя
 * разными правками. Теперь оттенок меняется здесь — и меняется везде.
 *
 * Значения продублированы в CSS-переменных (`index.css`) только там, где Tailwind
 * требует литерал в имени класса (`hover:bg-[...]`): переменную туда не подставить.
 */
export const UI = {
  /** Фосфор. Текст, рамки, живые линии. */
  PRIMARY: '#7fd6ff',
  /** Погасший фосфор: подписи, единицы, неактивное. */
  DIM: '#3f7391',
  /** Внимание: перегрев, низкое топливо. */
  WARN: '#ffb15c',
  /** Опасность: корпус пробит, ракета в воздухе. */
  DANGER: '#ff6a4a',
  /** Захваченная цель. Жёлтый — единственный, что не спорит с голубым. */
  TARGET: '#ffd75c',

  /**
   * Метки локатора. Цвет отвечает на вопрос «что это», и ответ должен быть один
   * и тот же на локаторе, на карте системы и в кабине.
   *
   * Звезда — жёлтая, причал — белый (рукотворное среди природного), планета —
   * фосфор консоли (`PRIMARY`). Корабли: враг красный (`DANGER`), свой зелёный,
   * чужой-но-не-враг серо-голубой (`NEUTRAL`). Камень — серо-бурый, груз —
   * оранжевый (`WARN`): и то и другое подбирают, но одно сперва расстреливают.
   *
   * Захваченная цель НЕ перекрашивается: жёлтым теперь горит звезда, и второй
   * жёлтый на локаторе сделал бы каждый бой вопросом «это цель или светило?».
   * Захват показывается кольцом вокруг отметки — формой, а не цветом.
   */
  STAR: '#ffd75c',
  STATION: '#ffffff',
  ALLY: '#5ce08a',
  ROCK: '#6f6558',
  /**
   * Особое состояние корабля: маскировка. Цель навигации им больше не красится —
   * она носит цвет своего тела, а выделяется ромбом и кольцом на локаторе.
   */
  NAV: '#b79cff',
  /** Обломок в пространстве: трофей, а не угроза. */
  SALVAGE: '#8fdcc0',
  /**
   * Мирный корабль. Не красный и не жёлтый: на радаре цвет обязан отвечать на
   * единственный вопрос боя — стрелять или нет. Спокойный серо-голубой говорит
   * «не твоё дело» быстрее, чем любая надпись.
   */
  NEUTRAL: '#9fb3c8',
  /**
   * Живой игрок (не бот). Розовый — чтобы человек читался на локаторе мгновенно и
   * не путался с NPC-торговцем (`NEUTRAL`): в общем мире важно с одного взгляда
   * отличить «там кто-то настоящий» от рядового трафика. Отдельный вопрос от «свой/враг».
   */
  PLAYER: '#ff8fd6',
} as const

/** Тот же фосфор для three.js, где цвет — число, а не строка. */
export const UI_HEX = {
  PRIMARY: 0x7fd6ff,
} as const
