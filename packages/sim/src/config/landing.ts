/** Посадка на планеты и луны без отдельного режима шасси. */
export const LANDING = {
  /** Любая новая положительная тяга после сброса рукояти начинает взлёт. */
  TAKEOFF_THROTTLE: 0.01,
  /** Начальный импульс посадочных двигателей наружу от поверхности, м/с. */
  TAKEOFF_SPEED: 30,
  /** Зазор после взлёта, чтобы следующим шагом снова не поймать поверхность, м. */
  RELEASE_GAP: 2,

  /**
   * Автопосадка. На PROMPT_HI над поверхностью загорается жёлтый пуш «нажми L»; нажать
   * можно, пока высота не упала ниже PROMPT_LO. Не успел — коснулся грунта неуправляемо
   * и разбился. Нажал — включается непрерываемый спуск (`stepAutoland`): корабль сходит
   * вниз со скоростью DESCENT_SPEED, ровняясь по касательной (LEVEL_RATE — доля довороту
   * за шаг), и садится, едва зазор до поверхности станет меньше CONTACT_GAP.
   */
  PROMPT_HI: 200,
  PROMPT_LO: 50,
  DESCENT_SPEED: 22, // м/с — управляемый сход, без инерции
  LEVEL_RATE: 3.5, // 1/с — темп доворота корпуса в касательную при спуске
  CONTACT_GAP: 2, // м над поверхностью, на котором фиксируем посадку
} as const
