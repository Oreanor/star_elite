/** Ховер над поверхностью: полёт по сфере, не касание грунта. */
export const LANDING = {
  /**
   * Автозаход. С APPROACH_HI — жёлтый пуш «подготовка к ховер-режиму»;
   * с PROMPT_HI — «нажмите L». L сажает на HOVER_ALT: своя физика по сфере
   * (тяга → вдоль поверхности). Отрыв — крейсер ≥ ESCAPE_CRUISE или L.
   */
  /** С этой высоты — жёлтое «подготовка к ховер-режиму», м. */
  APPROACH_HI: 1_000,
  /**
   * Потолок ВЫСОТОМЕРА, м. Выше него полоска у локатора молчит: это уже не полёт над
   * телом, а полёт в системе, и высота там не величина, за которой следят. Двадцать
   * километров — примерно с этой высоты крупная луна перестаёт быть «землёй под ногами»
   * и становится шаром в кадре.
   */
  ALTIMETER_HI: 20_000,
  /** С этой высоты — «нажмите L»; верх окна входа в ховер, м. */
  PROMPT_HI: 600,
  /** Низ окна входа в ховер, м. */
  PROMPT_LO: 400,
  /** Высота, на которую становится борт при входе в ховер, м. Дальше её ведёт пилот. */
  HOVER_ALT: 500,
  /**
   * Набор и снижение в ховере, м/с. Полсотни метров в секунду: с пятисот до самой земли
   * десять секунд — не мучительно и не мгновенно, высоту успеваешь поймать на глаз.
   */
  ALT_RATE: 50,
  /** Ниже не пустит рельс: борт втёрся бы брюхом в грунт. */
  ALT_MIN: 60,
  /** Выше ховер теряет смысл — это уже обычный полёт, и высотомер там молчит. */
  ALT_MAX: 20_000,
  /** Допуск «уже на высоте ховера» при автозаходе, м. */
  HOVER_SNAP: 5,
  DESCENT_SPEED: 40, // м/с — сход/подъём к HOVER_ALT на автозаходе
  LEVEL_RATE: 3.5, // 1/с — доворот брюхом к нормали на заходе

  /**
   * Множитель крейсера для отрыва. До порога — быстрый ховер по сфере;
   * выше — обычная физика, летишь куда нос/скорость смотрели.
   * ln(1000)/CHARGE_RATE(1.2) ≈ 5.8 с удержания форсажа.
   */
  ESCAPE_CRUISE: 1_000,

  /**
   * Стоянка только при обычном размере борта. Окна высот (PROMPT/HOVER) заданы в
   * метрах и с миелофоном дают ложные «подготовка / крушение» у камня сбоку —
   * выше 1× режим режем целиком.
   */
  MAX_SHIP_SCALE: 1,

  /**
   * Поверхность для стоянки: радиус ≥ стольких корпусов пилота (effectiveRadius).
   * Мельче не «притягивает» — ни пуш, ни L. Для глыб — к тверди (MESH_SOLID).
   */
  ASTEROID_MIN_SCALE: 10,

  /**
   * Неровный меш глыбы/астероида: доля радиуса для тверди и высоты стоянки.
   * Bounding sphere — пики; тело глубже.
   */
  MESH_SOLID: 0.75,

  /**
   * Неуправляемый удар о твердь: отскок БЕЗ урона.
   * Восстановление < 1 — теряешь ход. Потолок режет остаток крейсера.
   */
  CRASH_BOUNCE: 0.35,
  /** Макс. скорость после отскока, м/с. */
  CRASH_BOUNCE_MAX: 140,
} as const
