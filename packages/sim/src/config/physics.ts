/** Единицы: метры, секунды, радианы, тонны. */
export const PHYSICS = {
  /** Фиксированный шаг симуляции, с. Рендер к нему не привязан. */
  FIXED_DT: 1 / 120,
  /** Потолок накопителя, с: свёрнутая вкладка не должна телепортировать мир. */
  MAX_FRAME_DT: 1 / 30,
  /** Радиус, за которым мир сдвигается обратно к нулю (борьба с потерей точности float32), м. */
  FLOATING_ORIGIN_RADIUS: 4000,
  /**
   * Ручник (Ctrl): vel *= exp(−RATE·dt). ~0.3 с гасит любой ход (и ×scale, и крейсер)
   * почти в ноль — ретро вдоль носа на гигантском масштабе уже не справляется.
   */
  HANDBRAKE_RATE: 20,
  /**
   * Выбег на нулевом газе при FA, с⁻¹. Штатный ASSIST_SPEED_DAMP (~0.35) хорош для
   * плавной рукояти, но на ×scale хвост e^(−k·t) к абсолютным 0.5 м/с тянется десятки
   * секунд — «отпустил ПКМ, а скорость почти не падает». На выбеге берём этот темп.
   */
  COAST_RATE: 4,
} as const
