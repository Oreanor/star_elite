/**
 * Корабли поколений — «киты».
 *
 * Города размером в километры, идущие сквозь систему тысячелетним рейсом. Редкие,
 * мирные, неуязвимые: чистая декорация, задающая масштаб. Встретив такой, пилот
 * должен ощутить, что он в этой системе — букашка.
 */
export const TITAN = {
  /**
   * Доля встреч, оборачивающихся китом вместо кораблей. Кит — не рядовая встреча,
   * а событие: одна на шесть-семь. Чаще — примелькается и перестанет поражать.
   */
  ENCOUNTER_SHARE: 0.15,

  /** Сколько китов разом в системе. Два города в кадре — уже парк аттракционов. */
  MAX: 1,

  /** Сколько обликов рисует рендер. Спавн бросает вид в этих пределах. */
  VARIANTS: 3,

  /**
   * Марки по облику — их и подписывает HUD. Это КЛАСС корабля-города, а не имя
   * борта: китов одного облика в галактике много, как «боингов». Индекс совпадает
   * с обликом геометрии (линейный крейсер, ковчег-хребет, готический лайнер).
   */
  MARKS: ['«Левиафан»', '«Ковчег»', '«Вавилон»'] as readonly string[],

  /** Габарит, м. Десятки длин корабля — «крупнее в десятки раз», как и просили. */
  RADIUS: 1_600,

  /** Дрейф, м/с. Медленно: это город, а не курьер. Он ПЛЫВЁТ. */
  DRIFT_SPEED: 45,

  /** Продольное вращение, рад/с. Едва заметное — исполинам спешить некуда. */
  SPIN: 0.01,

  /** На каком удалении от игрока появляется. Виден силуэтом, не сваливается на голову. */
  SPAWN_RANGE: 11_000,

  /** Дальше этого — исчезает. Больше корабельного: кита видно с большего расстояния. */
  DESPAWN_RANGE: 26_000,

  /** Доля китов, что не летят мимо, а висят у станции. Остальные дрейфуют сквозь. */
  STATION_SHARE: 0.4,
} as const
