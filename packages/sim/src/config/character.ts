/**
 * Правила создания персонажа: что настраивается и во что это обходится.
 *
 * Данные, а не ветвление в UI. По ним и экран рисует раздачу очков, и СЕРВЕР
 * проверяет присланный профиль: статы нельзя накрутить правкой, как нельзя
 * «править HP через консоль» — тот же принцип авторитетности из ТЗ.
 *
 * Числа тут — заготовка под баланс, их и крутить. Раздаются ЧИСЛОВЫЕ оси персоны
 * (`intellect/charisma/willpower/agility/accuracy`). Тона (`disposition/secrecy/
 * humor`) и `temperament` очков не стоят — это окраска, а не сила.
 */
export const CHARACTER = {
  /** Стартовое значение каждой черты до раздачи очков. */
  BASE: 2,
  /** Дно и потолок черты (шкала персоны 1..5). */
  MIN: 1,
  MAX: 5,
  /** Очков на распределение сверх базы. */
  POOL: 6,
  /**
   * Цена ОДНОЙ единицы черты в очках. Коэффициент: дорогая черта съедает пул
   * быстрее. По умолчанию всё по 1 — крутится здесь, а не условием в интерфейсе.
   * Ключи — ровно те оси, что раздаются; они же перечислены в `BUYABLE_TRAITS`.
   */
  COST: {
    intellect: 1,
    charisma: 1,
    willpower: 1,
    agility: 1,
    accuracy: 1,
  },
} as const
