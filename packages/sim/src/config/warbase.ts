/**
 * Военная база — большой сферический объект на снос. Данные, не хардкод: где и какого
 * размера базы стоят, задаёт система (`SystemDef.warBases`), а не эта таблица. Здесь —
 * общие для всех баз числа: сколько на ней навесных деталей, их калибр, прочность корки.
 */
export const WARBASE = {
  /** Собственное вращение базы, рад/с. Медленное — это станция, а не волчок. */
  SPIN: 0.015,
  /**
   * Прочность корпуса на снос ∝ радиусу: километровую базу нельзя ронять одним залпом,
   * как мелкий камень. База радиуса R (м) держит `HULL_PER_KM · R/1000` урона.
   */
  HULL_PER_KM: 4_000,

  /** Навесных деталей на базу: от и до. Точное число — из сида базы. */
  FIXTURES_MIN: 20,
  FIXTURES_MAX: 40,
  /**
   * Калибр детали относительно РАДИУСА КОРПУСА, доля. Детали примерно с корабль или
   * меньше: на километровой базе это мелкие бугры, вблизи — узнаваемые пушки и ангары.
   */
  FIXTURE_SIZE_MIN: 0.012,
  FIXTURE_SIZE_MAX: 0.03,
  /** Башня на полюсе крупнее рядовой детали — это доминанта силуэта. */
  TOWER_SIZE: 0.06,
} as const
