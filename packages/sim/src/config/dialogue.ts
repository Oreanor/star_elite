/**
 * Разговор. Числа переговоров — здесь, слова и правила — в домене.
 *
 * Всё, что решается броском кости, решается ОДИН раз, на реплику. Внутри шага
 * физики диалога не бывает: он событие, а не процесс.
 */
export const DIALOGUE = {
  /**
   * Дальше этого собеседник не слышит, м. Совпадает с `AI.AWARENESS`: разговаривать
   * можно ровно с теми, кто тебя уже видит.
   */
  RANGE: 3500,

  /**
   * Пират сдаётся тем охотнее, чем сильнее избит. При целом корпусе шанс нулевой:
   * невредимый пират не бросает добычу, и уговаривать его бессмысленно.
   *
   * Шанс = (1 − доля корпуса) × множитель. Это не «сложность», а торг: сначала
   * сбей ему щит и покорёжь корпус — потом требуй.
   */
  SURRENDER_GAIN: 0.9,

  /**
   * Мольба о пощаде. Пират отпускает того, с кого нечего взять, и добивает того,
   * кто при смерти. Поэтому шанс растёт с ТВОИМ здоровьем: умирающему не верят,
   * его добивают.
   */
  MERCY_BASE: 0.15,
  MERCY_HEALTH_GAIN: 0.45,
  /** Отдать груз — веский довод. Прибавка к шансу, если трюм не пуст. */
  MERCY_CARGO_BONUS: 0.3,

  /**
   * Торговец подчиняется требованию сбросить груз, только если напуган: щит сбит
   * или корпус повреждён. Целого и невредимого разбоем не запугать — он знает,
   * что станция рядом.
   */
  PIRACY_FEAR_THRESHOLD: 0.75,

  /**
   * Словесная угроза (требование сбросить груз целому торговцу) весит столько
   * «попаданий» в счётчике обиды (`ai.grievance`). При пороге `GRIEVANCE.HOSTILE_HITS`
   * = 4 двух наглых требований довольно, чтобы нейтрал перестал терпеть и встал на бой.
   * Тот же счётчик, что и у попаданий, — угроза словом и выстрел копятся вместе.
   */
  THREAT_WEIGHT: 2,

  /**
   * Оскорбление словом весит столько же «попаданий»: нахамить — не то же, что
   * пригрозить грабежом, но копится в тот же счётчик обиды и на пороге рвёт мир
   * (нейтрал во враги, эскорт — прочь). Распознаёт хамство модель, следствие — движок.
   */
  INSULT_WEIGHT: 2,

  /**
   * Насколько НРАВ и ВОЛЯ собеседника двигают шанс сломаться, ± к доле. Волевой и
   * храбрый упирается дольше (сдаётся реже, сопротивляется грабежу даже избитым),
   * трусливый и слабовольный подаётся раньше. Слабость честная — характером, а не
   * заниженным здоровьем. Стойкость считается в `nerve()`, −1..+1.
   */
  NERVE_SWING: 0.25,

  /**
   * Плата наёмнику, кредитов. Разово, вперёд. Это БАЗА: настоящую цену двигают нрав и
   * отношение (см. `escortFee`) — договор это цена И согласие, а не только деньги.
   */
  ESCORT_FEE: 800,
  /** Жадный дерёт за сопровождение дороже базы. */
  ESCORT_FEE_GREEDY: 1.6,
  /** Расположенный к тебе (warm) уступит в цене. */
  ESCORT_FEE_FRIENDLY: 0.6,
  /**
   * Выучка наёмника. Половина: реакция вдвое медленнее, разброс вдвое шире.
   * Урон и угловая скорость у него те же, что у всех, — иначе это была бы
   * не слабость пилота, а поблажка физики.
   */
  ESCORT_SKILL: 0.5,
} as const
