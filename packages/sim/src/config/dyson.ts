/**
 * Сферы Дайсона — мегаструктуры вокруг звёзд. Чистая декорация: их строит не
 * механика, а цивилизация. Появляются лишь у самых развитых обитаемых систем —
 * каркас вокруг светила и есть зримый знак высокой технологии.
 *
 * Радиус задан в РАДИУСАХ звезды, а не в метрах: у карлика и у гиганта структура
 * своя, но всегда в нескольких радиусах над короной — там, где её и видно,
 * подлетев к звезде, а не за астрономическую единицу, где она слилась бы с фоном.
 */
export const DYSON = {
  /** Сколько обликов рисует рендер: каркасная сфера, обод-кольцо, рой панелей. */
  VARIANTS: 3,

  /** Тех-уровень столицы, ниже которого сферу не строят. Только вершина прогресса. */
  MIN_TECH: 12,

  /** Доля систем-кандидатов (тех-уровень достаточен), у которых сфера есть. */
  CHANCE: 0.45,

  /**
   * Шанс, что у СКРОМНОЙ обитаемой системы (жизнь есть, но тех-уровень невысок)
   * стоят РУИНЫ сферы. Останки цивилизации, что однажды поднялась до звёзд и пала:
   * сфера пережила строителей. Мала намеренно — это редкость на редкость.
   */
  RUINED_CHANCE: 0.006,

  /** Радиус структуры в радиусах звезды. Несколько — она обнимает корону, не касаясь. */
  SHELL_RADIUS: 3,

  /** Вращение структуры, рад/с. Медленное: мегаструктуре спешить некуда. */
  SPIN: 0.006,

  /**
   * Доля структуры, УЦЕЛЕВШАЯ у руин, 0..1. Половина панелей выбита, каркас
   * прорежен — сразу видно, что это останки, а не стройка. Разрушение
   * детерминировано (по индексу панели), а не случайно: руина не мерцает.
   */
  RUIN_SURVIVAL: 0.5,
} as const

/** Что за сфера у звезды: облик и цела ли она. */
export interface DysonSpec {
  readonly variant: number
  readonly ruined: boolean
}
