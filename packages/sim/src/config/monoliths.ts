/**
 * Монолиты — исполинские статуи у причала.
 *
 * Чистая декорация масштаба, как и киты: висят, ничего не делают, ни с чем не сталкиваются.
 * Смысл их — дать глазу мерило. Корабль в 26 метров рядом со стокилометровым Люцифером
 * читается пылинкой, и система перестаёт быть пустым чёрным полем.
 */
export const MONOLITH = {
  /**
   * Сколько обликов рисует рендер. Расстановка кладёт по одному каждого — то есть это же
   * число и есть КОЛИЧЕСТВО статуй у причала. Облики живут в `statueGlb.ts`.
   */
  VARIANTS: 3,

  /**
   * Габарит, м — радиус, то есть поперечник вдвое больше: СТО КИЛОМЕТРОВ.
   *
   * Вдесятеро против первых десяти километров. Всё остальное здесь задано В ДОЛЯХ этого числа,
   * поэтому статуя растёт вместе со своим двором и одна правка не рассыпает расстановку.
   */
  RADIUS: 50_000,

  /**
   * Кувырок, рад/с. МЕДЛЕННЫЙ — вчетверо тише станции (у той 0.08): статуя не механизм,
   * ей крутиться незачем. Полный оборот выходит примерно за пять минут — движение заметно,
   * только если задержать взгляд, и это ровно то ощущение, которое нужно.
   */
  SPIN: 0.02,

  /**
   * На каком удалении от причала стоят, м — кратно своему радиусу. Достаточно близко, чтобы
   * попадать в кадр вместе со станцией, и достаточно далеко, чтобы не загораживать её.
   */
  STATION_GAP_MIN: 2.5,
  STATION_GAP_MAX: 4.0,

  /**
   * Пояс камней у Люцифера. Взрыв оставляет подбираемые осколки с рудой.
   * Облики — `scenicRockGlb.ts`; мелочь рисует рендер простыми мешами с их текстурой.
   */
  ROCK_SHAPES: 2,
  /** Сколько камней в поясе. */
  ROCK_COUNT: 56,
  /** Габарит камня, м — радиус. Поперечник выходит 1…2 км. */
  ROCK_RADIUS_MIN: 500,
  ROCK_RADIUS_MAX: 1_000,
  /** Удаление от центра статуи в долях её радиуса: сразу за силуэтом и чуть дальше. */
  ROCK_GAP_MIN: 1.15,
  ROCK_GAP_MAX: 2.6,
  /** Толщина пояса по нормали к плоскости, в долях радиуса статуи. */
  ROCK_THICKNESS: 0.35,
  /** Кувырок камня, рад/с — чуть живее статуи, всё ещё медленно. */
  ROCK_SPIN: 0.04,
  /**
   * Прочность самого мелкого камня. Крупный получает пропорционально радиусу:
   * с пульсаром (~9) мелкий держит десяток попаданий, крупный — около двадцати.
   */
  ROCK_HULL: 90,
  /** Масштаб вспышки гибели относительно радиуса камня. */
  ROCK_BLAST: 0.08,
  /** Осколков с самого мелкого / самого крупного камня. Линейно по радиусу. */
  ROCK_DEBRIS_MIN: 3,
  ROCK_DEBRIS_MAX: 12,
  /** Масса одного осколка, т (единицы руды). */
  ROCK_DEBRIS_MASS: 2,
  /** Визуальный радиус осколка, м — мелкий щебень рядом с километровой глыбой. */
  ROCK_DEBRIS_RADIUS: 6,
  /** Разлёт осколков от центра, м/с. */
  ROCK_DEBRIS_SPEED: 28,
} as const
