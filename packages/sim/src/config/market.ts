/**
 * Рынок. Цена товара НЕ назначается числом на систему — она выводится из уровня
 * развития мира и его строя, плюс колебание спроса и предложения. Одна формула на
 * все категории; категорию отличает только её «родной» тех-уровень (tier у товара).
 *
 * Смысл: мир дёшев в том, что производит, и дорог в том, что ввозит. Аграрий сбывает
 * еду за бесценок и переплачивает за электронику; у высокотеха — наоборот. Возить
 * выгодно ОТТУДА, где дёшево, ТУДА, где дорого, — а не через соседний прилавок.
 */
export const MARKET = {
  /**
   * Спред между покупкой и продажей на ОДНОЙ станции. Купил и тут же продал —
   * потерял этот процент: прибыль обязана приходить из перевозки, а не из прилавка.
   */
  SPREAD: 0.16,

  /** Скидка у мира-производителя: где товар родной, он дешевле каталога. */
  PRODUCER_DISCOUNT: 0.72,

  /**
   * Наценка за ввоз ГОТОВОГО хайтека в отсталый мир, на единицу разрыва тех-уровня.
   * Круче встречной: сделать компьютер там, где нет заводов, нельзя вовсе, а сырьё
   * развитый мир хотя бы добывает сам.
   */
  IMPORT_SLOPE: 1.5,

  /** Наценка за потребление СЫРЬЯ развитым миром, на единицу разрыва. Пологая. */
  CONSUME_SLOPE: 0.6,

  /** Цена контрабанды у анархии (дёшево, открыто) и у порядка (дорого, из-под полы). */
  CONTRA_LOW: 0.55,
  CONTRA_HIGH: 1.8,

  /** Опорный запас, ед. От него считается дефицит: столько — «в норме». */
  REF_STOCK: 90,

  /** Разброс запаса: множитель к опорному, случайный, но детерминированный из зерна. */
  STOCK_JITTER_MIN: 0.4,
  STOCK_JITTER_MAX: 2.0,

  /** Пределы влияния запаса на цену. Пусто на складе дорого, но не бесконечно. */
  SCARCITY_MIN: 0.82,
  SCARCITY_MAX: 1.28,

  /** Ширина шкалы тех-уровня (1..15): разрыв делится на неё, чтобы наклон был в долях. */
  TECH_SPAN: 14,
} as const
