/**
 * Знакомые как контакты галактики. Числа их закулисной жизни — здесь.
 *
 * За кулисами контакт живёт СКРОМНО, не полноценно: он не торгует, не воюет,
 * не считает деньги. Всё, что он делает вне поля зрения игрока, — изредка
 * перелетает в соседнюю систему и совсем редко гибнет. Один «ход» этой жизни
 * приходится на КАЖДЫЙ прыжок игрока: прыжок — это отрезок времени, и за него
 * галактика чуть-чуть смещается. Внутри шага физики контактов нет — это событие.
 */
export const CONTACTS = {
  /**
   * Праздный знакомый (без обещаний и заданий) с этим шансом за прыжок игрока
   * снимается с места и уходит в соседнюю систему. Не каждый ход: иначе контакты
   * метались бы по галактике, и «где он» менялось бы быстрее, чем игрок успевает
   * прочитать. Связанный обещанием (`boundFor`) не бродит — он идёт к цели.
   */
  WANDER_CHANCE: 0.4,

  /**
   * Как далеко достаёт один перелёт контакта, световых лет. Порядка обычного
   * привода (`GALAXY.BASE_JUMP_RANGE` = 27): контакт летает на том же топливе,
   * что и все, а не прыгает через полгалактики за ход.
   */
  WANDER_RANGE_LY: 30,

  /**
   * Шанс за прыжок игрока, что знакомый вне поля зрения ПОГИБНЕТ — нарвался на
   * пиратов, на звезду, на невезение. Редко: галактика знакомых должна редеть
   * годами, а не за вечер. Гибель необратима — контакт перестаёт отвечать, и
   * игрок получает весть «пропал с радара».
   */
  DEATH_CHANCE: 0.015,

  /** Сколько секунд висит весть о пропавшем контакте, прежде чем погаснуть. */
  NOTICE_LIFE: 9,

  /**
   * Доля знакомых, что при входе в систему выходит из гиперпрыжка у причала (как
   * игрок), а не подлетает своим ходом с кромки радара. Только если в системе есть
   * станция; иначе все заходят с предела видимости.
   */
  RESIDENT_HYPER_SHARE: 0.5,
} as const
