/**
 * Слабости ИИ моделируются честно — задержкой, разбросом и инерцией,
 * а не заниженным уроном. Физика у него ровно та же, что у игрока.
 */
export const AI = {
  /** Дальше — бот не замечает цель, м. */
  AWARENESS: 3500,
  /** Ближе — переходит в атаку, м. */
  ENGAGE: 2400,
  /** Ближе — разрывает атаку, иначе таранит, м.
   *  Держим низким: на дистанции 300+ м угловой размер цели меньше двух градусов,
   *  и бой превращается в переглядывание. Настоящая собачья свалка — вблизи. */
  BREAK_OFF: 120,
  /** Длительность манёвра отрыва, с. */
  BREAK_TIME: 2.6,
  /** Базовый полуугол конуса открытия огня, рад. Расширяется с дистанцией. */
  FIRE_CONE: 0.045,
  FIRE_RANGE: 1500,
  /** Разброс прицела, рад на метр дистанции. Без него бот — снайпер:
   *  при 0.012 промах выходил меньше радиуса корабля, то есть промаха не было.
   *  0.03 давало смещение 4.5 м на 300 м при радиусе цели 12 — тоже попадание.
   *  Разброс УГЛОВОЙ, поэтому доля попаданий не зависит от дистанции: так и задумано. */
  FIRE_SPREAD: 0.055,
  /** Как часто бот пересматривает решение, с. Это его время реакции. */
  THINK_INTERVAL: 0.16,
  /** Фора текущей цели при выборе новой. <1 — прилипчивость.
   *  Бот, меняющий жертву каждый кадр, не доводит ни одной атаки до конца. */
  TARGET_STICKINESS: 0.7,
  /**
   * Тяга в атаке. Низкая намеренно: угловая скорость линии визирования
   * равна v/d, и на 200 м/с ни один корабль не удержит цель в прицеле.
   * Собачья свалка — это медленно.
   */
  ATTACK_THROTTLE_FAR: 0.85,
  ATTACK_THROTTLE_NEAR: 0.4,
  /** Дистанция, ближе которой бот сбрасывает тягу, м. */
  ATTACK_SLOW_RANGE: 700,
  /** Как часто обновляется дрожание прицела, с. Чаще — получится белый шум. */
  AIM_JITTER_INTERVAL: 0.35,
  /** Пороги выхода из боя. */
  EVADE_HULL_FRAC: 0.35,
  EVADE_SHIELD_FRAC: 0.1,
  /** Форсаж при отрыве и уклонении. */
  ESCAPE_BOOST: 1.35,

  /**
   * Минимум секунд между пусками ракет.
   *
   * Раньше решение принималось КАЖДЫЙ ШАГ ФИЗИКИ с вероятностью 0.004 — это
   * почти пол-ракеты в секунду. Бот высыпал четыре ракеты за двадцать секунд,
   * то есть 220 урона при 190 живучести игрока: смерть наступала от ракет,
   * а не от боя. Плюс темп зависел от частоты шага.
   */
  MISSILE_INTERVAL: 14,
  /** Вероятность пуска в такте размышления, когда перезаряд истёк. */
  MISSILE_CHANCE: 0.35,
  /** Ближе и дальше этого ракету не пускают: вблизи не наведётся, вдали увернутся. */
  MISSILE_MIN_RANGE: 500,
  MISSILE_MAX_RANGE: 1400,

  /**
   * ПРО. Бот жмёт импульс, когда ракета уже близко: издали дешевле увернуться.
   * Дальность 300 м ракета проходит примерно за 0.7 с — это 4–5 тактов
   * размышления, и при шансе 0.15 за такт до цели доходит около половины ракет.
   * Ровно этого и хотим: ракета опасна, но не гарантирована.
   */
  ECM_RANGE: 300,
  ECM_CHANCE: 0.15,
} as const

/**
 * Боевой дух и решение бежать.
 *
 * Бот не бросает бой по одному порогу здоровья — страх КОПИТСЯ и складывается из
 * трёх факторов: своя слабость, сила противника и робость характера. По мере того
 * как бот слабеет, сумма растёт и однажды переваливает `FLEE`. Но если и противник
 * почти добит — это перевешивает (входит со знаком минус): добить выгоднее, чем
 * бежать. Веса подобраны так, что нейтральный характер при равном враге срывается
 * с боя около трети корпуса (как прежний жёсткий порог), трус — раньше, храбрец
 * дерётся дальше, а сильный противник сдвигает срыв к большему запасу здоровья.
 */
export const MORALE = {
  /** Веса вклада в «свою слабость», 0..1. Сумма при полном здоровье гасит её в ноль. */
  HULL_W: 0.6,
  SHIELD_W: 0.25,
  ENERGY_W: 0.15,

  /** Веса трёх слагаемых страха и противовеса — слабости врага. */
  OWN_W: 1.0,
  ENEMY_W: 0.4,
  COWARD_W: 0.5,
  ENEMY_WEAK_W: 0.6,

  /** Насколько сильный враг вообще способен напугать: потолок относительной силы. */
  ENEMY_STR_CAP: 1.5,

  /** Робость от нрава: трус боязлив, храбрец — наоборот; воля сдвигает. */
  COWARD_BASE: 0.6,
  BRAVE_BASE: -0.4,
  WILL_WEIGHT: 0.15,

  /** Порог перехода в бегство и гистерезис возврата: без него бот дёргался бы у порога. */
  FLEE: 0.6,
  HYSTERESIS: 0.15,
} as const

/**
 * Обида: реакция не-враждебного борта на попадания ИГРОКА.
 *
 * Случайно задеть встречного — не то же, что напасть. Одиночное попадание не делает
 * из нейтрала врага: у пилота копится ПРЕТЕНЗИЯ, он вызывает игрока по связи и ждёт
 * объяснений (это читает UI по `grievance>0`). Разрядить можно словом или кнопкой
 * «случайно». Но если продолжать жать на гашетку — на `HOSTILE_HITS`-м засчитанном
 * попадании подряд пилот считает нападение намеренным и становится врагом уже честно.
 *
 * Числа — тут; сборка решения в `combat/grievance.ts`.
 */
export const GRIEVANCE = {
  /** На этом засчитанном попадании подряд не-враг становится врагом: «больше трёх раз». */
  HOSTILE_HITS: 4,
  /**
   * Минимум секунд между ЗАСЧИТАННЫМИ попаданиями. Непрерывно вести лучом по борту —
   * одно событие, а не десять по кадру: иначе полусекундный чирк мгновенно копил бы
   * враждебность. Отдельные «тычки» с паузой считаются каждый.
   */
  HIT_DEBOUNCE: 0.5,
  /**
   * Секунд без новых попаданий, после которых претензия гаснет сама. Один случайный
   * выстрел не висит на пилоте вечно: перестал стрелять — он списал это на нелепость.
   */
  COOLDOWN: 8,
} as const

/**
 * Побег из системы гиперпрыжком.
 *
 * Напуганный бот, которому есть на чём (стоит гипердвигатель), может не просто
 * отлететь, а уйти из системы совсем. Держим РЕДКИМ намеренно: если бы все, кого
 * ты пугаешь, разбегались прыжком, космос читался бы как «от игрока только и
 * делают, что сваливают». Зарядка занимает секунды — успей добить, пока он мигает.
 */
export const WARP = {
  /** Секунд зарядки прыжка-побега. Пока заряжается — уязвим и ещё в системе. */
  CHARGE: 4,
  /** Шанс в такт размышления НАЧАТЬ побег, когда напуган и есть привод. Низкий: редкость. */
  CHANCE: 0.03,
  /** Секунд после попадания, в течение которых мирный считает себя под огнём. */
  THREAT_WINDOW: 6,
  /** Сколько живёт вспышка перехода, с. Одинаково на вход и на выход. */
  FLASH_LIFE: 0.8,
} as const
