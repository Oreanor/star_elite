/**
 * Пиратская платформа-гнездо.
 *
 * Стационарный «авианосец» упрощённой формы, на палубе которого дремлет звено
 * пиратов. Подойди ближе WAKE_RANGE открыто — они стартуют и атакуют; подойди
 * под маскировкой — и вырежешь их прямо на палубе, гнездо не проснётся. Саму
 * платформу можно расстрелять в металл, но взрыв сжигает бо́льшую часть груза.
 *
 * Встречается РЕДКО и играет роль среднего врага: не рядовой пират у причала, но
 * и не событие масштаба кита. Числа держим здесь — балансировщик крутит их первыми.
 */
export const PLATFORM = {
  /**
   * Доля встреч, оборачивающихся платформой вместо кораблей. Реже пиратов: гнездо —
   * находка, а не рядовой патруль. Одна на десяток-полтора встреч.
   */
  ENCOUNTER_SHARE: 0.09,

  /** Сколько платформ разом в системе. Одна: два гнезда в кадре — уже не находка. */
  MAX: 1,

  /** Сколько обликов рисует рендер. Спавн бросает вид в этих пределах. */
  VARIANTS: 1,

  /** Пиратов на палубе. Звено: вырезать поодиночке под полем — на несколько заходов. */
  CREW_MIN: 3,
  CREW_MAX: 4,

  /**
   * Прочность корпуса платформы. Крепость, а не истребитель: несколько заходов
   * лазером. Не щит — голая обшивка, поэтому число сопоставимо с корпусом баржи.
   */
  HULL: 600,

  /**
   * Радиус СФЕРЫ СТОЛКНОВЕНИЙ, м. Это ядро корпуса, по которому бьёт луч, — оно
   * заметно меньше видимого силуэта и меньше выноса палубы (см. DECK_RADIUS),
   * иначе луч, пущенный в спящего пирата на периферии, утыкался бы в саму
   * платформу и будил гнездо вместо тихого убийства.
   */
  RADIUS: 70,

  /** Габарит для локатора и отсечения, м. Силуэт крупнее ядра — это авианосец. */
  EXTENT: 170,

  /**
   * Вынос палубных мест от центра, м. Пираты сидят ЗА сферой столкновений
   * (RADIUS), чтобы их можно было отстреливать по одному, не задевая корпус.
   */
  DECK_RADIUS: 130,

  /**
   * На каком удалении от платформы, м, открытый (не в маскировке) игрок будит
   * экипаж. «Ближе двух километров — стартуют», ровно как просили.
   */
  WAKE_RANGE: 2_000,

  /**
   * Сколько трюмов игрока остаётся металлом после взрыва. Платформа — тонны
   * металла, но детонация сжигает бо́льшую часть: подбираемого ровно на столько
   * своих трюмов. Считается от вместимости игрока в момент гибели, не от числа
   * в тоннах: «три трюма» одинаково щедры и для скаута, и для грузовика.
   */
  SCRAP_HOLDS: 3,

  /** На сколько контейнеров бьётся уцелевший металл. Поле обломков, а не один куб. */
  SCRAP_PODS: 6,

  /** Масштаб взрыва гибнущей платформы. Крупнее корабельного: это крепость. */
  EXPLOSION_SCALE: 6,

  /** Где появляется относительно игрока, м. В пределах локатора: гнездо надо заметить. */
  SPAWN_RANGE: 7_500,

  /** Дальше этого от игрока — гнездо убирается вместе со спящим экипажем, м. */
  DESPAWN_RANGE: 18_000,
} as const
