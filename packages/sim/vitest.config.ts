import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    include: ['src/**/*.test.ts'],
    environment: 'node',
    /**
     * Стандартные 5 секунд для этой сюиты слишком тесны. Галактика — 2500 систем, и тесты,
     * которые строят её целиком (детерминизм зерна, доля двойных, редкость сфер Дайсона),
     * укладываются в них лишь пока машина свободна. Под параллельным прогоном файлов они
     * начинали падать по таймауту вразнобой — каждый раз разные, — и выглядело это как
     * плавающий баг генерации, хотя изолированно всё зелено.
     *
     * Резать сами проверки ради скорости нельзя: детерминизм зерна держит и сохранения,
     * и сеть. Дешевле дать времени.
     */
    testTimeout: 30_000,
  },
})
